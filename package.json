{
  "name": "ghost-office",
  "version": "1.0.0",
  "description": "Ghost Office — autonomous AI software company with 19 specialized agents, 55 skills, 24 commands, and safety guardrails for infrastructure, CI/CD, cloud operations, and reliable deployments.",
  "main": "scripts/lib/utils.js",
  "scripts": {
    "test": "node scripts/tests/run-all.js",
    "setup": "node scripts/setup.js",
    "validate": "node scripts/validate-structure.js",
    "lint:json": "node -e \"const fs=require('fs');const files=['.claude/settings.json','.mcp.json'];files.forEach(f=>{try{JSON.parse(fs.readFileSync(f,'utf8'));console.log('✅ '+f)}catch(e){console.error('❌ '+f+': '+e.message);process.exit(1)}})\""
  },
  "keywords": [
    "claude-code",
    "devops",
    "ai-agents",
    "infrastructure",
    "automation",
    "terraform",
    "kubernetes",
    "docker",
    "cicd",
    "github-actions",
    "microsoft-365"
  ],
  "author": "Bipin Kareparambil",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
